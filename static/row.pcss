@tailwind base;
@tailwind components;
@tailwind utilities;

.chip-row,.chip-top-row {
    @apply justify-center select-none;
    margin-left: -15px;
    margin-right: -15px;
}

.chip-top-row {
    @apply sticky top-0 z-10;
    background-color: gray;
}